// Logic is handled via event listeners, keeping HTML clean
const submitBtn = document.querySelector('#submitBtn');

submitBtn.addEventListener('click', () => {
    console.log('Action handled by the behavior layer.');
});

